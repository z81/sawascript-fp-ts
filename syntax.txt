a = 1 + 1

a '^ b = a * b

0..1

start '.. end = Array.range(0, 100)

Array = {
  range(start, end) = ...
}

{} блок скоп, последнее значение считается за return
() кортеж, разделитель , работает деструктурирование
.. функция которая возвращает диапазон чисел
= и + это функции
' в начале значит что это символ
